<h1>Lista de 5 productos random</h1><br>

{{#if producto}}

<table class="table">
    <thead class="table-dark">
        <th>Nombre</th>
        <th>Precio</th>
        <th>Foto</th>
    </thead>
    <tbody>
        {{#each producto }}
        <tr class="table-primary">
            <td>{{./title }}</td>
            <td>{{./price }}</td>
            <td><img style="width: 80px; height:80px" src={{./thumbnail }} alt=""></td>
        </tr>
        {{/each }}
    </tbody>
</table>
{{else }}
<p>No hay productos</p><br>
{{/if }}
<br><a href="/"><input type="button" value="Agregar"></a>
<hr>
